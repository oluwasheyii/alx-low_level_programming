#include "main.h"

/**
 * mul - Multiplies two integers
 * @a: The first integers
 * @b: The second integers
 * Return: results of integers
 */

int mul(int a, int b)
{
int result = a * b;
return (result);
}
